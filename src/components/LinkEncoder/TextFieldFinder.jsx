import React from 'react';
import PropTypes from 'prop-types';
import { Box } from '@mui/material';



function TextFieldFinder (props) {
  return <Box></Box>;
}

TextFieldFinder.propTypes = {
  textFieldData:PropTypes.String
};

export default TextFieldFinder;